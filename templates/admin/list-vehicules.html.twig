<div class="container col-offset-2 col-6">
    <h1>Liste des vehicules</h1>
    <div class="table-responsive col-12">
        <table class="table table-striped table-sm table-admin">
            <thead>
            <tr>
                <th>Id</th>
                <th colspan="2">Marque</th>
                <th colspan="2">Modele</th>
                <th colspan="2">Numéro série</th>
            </tr>
            </thead>
            <tbody>
            {% for vehicule in vehicules %}
                <tr>
                    <td>{{ vehicule.id }}</td>
                    <td>{{ vehicule.marque.nom }}</td>
                    <td>{{ vehicule.modele.nom }}</td>
                    <td>{{ vehicule.NumeroSerie }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
